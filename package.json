{
  "name": "tree-sitter-menhir",
  "version": "0.1.2",
  "description": "Tree-sitter parser for menhir files",
  "main": "index.js",
  "repository": "https://github.com/Kerl13/tree-sitter-menhir",
  "keywords": [
    "parser",
    "ocaml",
    "menhir"
  ],
  "scripts": {
    "build": "tree-sitter generate && node-gyp build",
    "test": "tree-sitter test && script/run_menhir_test_suite.sh"
  },
  "author": "Martin Pépin",
  "license": "MIT",
  "dependencies": {
    "nan": "^2.14.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.15.3"
  }
}
